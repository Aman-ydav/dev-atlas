import { TypeListPage } from "./TypeListPage";

export default function InterviewPage() {
    return (
        <TypeListPage
            type="interview"
            title="Interview"
            description="Frontend, backend, HR, and system design prep — with real project examples woven in."
        />
    );
}
